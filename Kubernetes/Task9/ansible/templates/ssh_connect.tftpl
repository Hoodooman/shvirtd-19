[jumpbox]
jumbox: ssh -i ~/.ssh/shvirtd-19_pvk ubuntu@${jumpbox.network_interface[0].nat_ip_address}

[masters]
%{ for idx, master in masters }
master-${idx}: ssh -i ~/.ssh/shvirtd-19_pvk ubuntu@${master.network_interface[0].nat_ip_address}
%{ endfor }

