apiVersion: v1
kind: Pod
metadata:
  name: multitool-test
spec:
  containers:
  - name: multitool-container
    image: wbitt/network-multitool:latest
    ports:
    - containerPort: 1180
    command: ["sleep", "infinity"]  # Чтобы Pod не завершал работу сразу
